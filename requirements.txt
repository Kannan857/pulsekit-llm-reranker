# Core application framework
fastapi
uvicorn[standard]
python-dotenv

# Reranker library
sentence-transformers

# Pin transformers to a version compatible with vllm v0.4.2
# This prevents sentence-transformers from installing an incompatible version.
transformers==4.39.3